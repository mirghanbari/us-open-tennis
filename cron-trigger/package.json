{
  "name": "uso-data-trigger",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker: drives the US Open data workflow's cadence, working around GitHub's schedule throttling.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "dry": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
